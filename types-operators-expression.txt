#inclue <stdio.h>
int main() {
    char str[10]; 
}

#inclue <stdiob.h>
main() {
    atoi(ตัวแปร);
    atod(ตัวแปร);
    atol(ตัวแปร);
}

ภาษาซีไม่มีค่าตัวแปรที่เป็นบูลลีน (True/False) แต่จะแทนด้วยตัวอื่นแทน เช่น
type int :
    int F = 0; (False)
    int T = 1; (True)
type char :
    char F = '\0'; (null charecter , terminate charecter)
    char T = 'a';
type float :
    float F = 0.0;
    float T = 1.0;

relational operator > >= < <= == !=
longical operator && || !
